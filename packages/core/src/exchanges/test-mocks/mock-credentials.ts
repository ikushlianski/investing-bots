import type { ExchangeCredentials } from '../types'

export const mockBinanceCredentials: ExchangeCredentials = {
  apiKey: 'test-binance-api-key',
  apiSecret: 'test-binance-api-secret',
  environment: 'testnet',
}

export const mockBybitCredentials: ExchangeCredentials = {
  apiKey: 'test-bybit-api-key',
  apiSecret: 'test-bybit-api-secret',
  environment: 'testnet',
}
